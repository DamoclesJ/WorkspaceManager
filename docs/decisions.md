# WorkspaceManager 决策记录

> 状态：草稿  
> 最后更新：2026-08-04  
> 说明：本文档按架构决策记录（ADR）方式记录重要决策，新决策应追加到末尾。

## 已记录的决策

### ADR-001: 当前阶段只维护文档，不实现功能

- 日期：2026-08-04
- 状态：已接受
- 背景：项目处于初始化阶段，硬件能力和技术方案尚未验证。
- 决策：先维护 `README.md` 和 `docs/`，不编写业务代码。
- 影响：文档记录方向；实现等关键问题确认后再开始。

### ADR-002: 暂不锁定技术栈

- 日期：2026-08-04
- 状态：暂定
- 背景：交付形态、控制端位置和硬件控制方式会显著影响技术选择。
- 决策：在完成硬件能力盘点前不提交技术栈相关配置。
- 影响：避免过早引入难以回退的工程结构。

### ADR-003: 产品定位为个人混合桌面环境自动化管理工具

- 日期：2026-08-04
- 状态：已接受
- 背景：早期将项目理解为开发工作区管理工具，方向错误。
- 决策：项目目标改为管理 Mac mini M4 + Windows 台式机的混合工作环境，覆盖远程电源、显示器输入、Windows 显示模式、Logitech Flow 和快捷控制。
- 影响：需求、架构和路线图均以桌面环境自动化为准。

### ADR-004: 本地优先，不依赖云服务

- 日期：2026-08-04
- 状态：暂定
- 背景：个人桌面环境操作通常应在本机局域网内完成。
- 决策：控制、状态和通信默认只在本地局域网内进行。
- 影响：无需账号和云基础设施，但需要处理局域网发现和鉴权。

### ADR-005: MVP 采用固定命令流程，不依赖显示器状态读回

- 日期：2026-08-08
- 状态：已接受
- 背景：Phase 0 已验证两条可靠的显示切换流程：Windows 的 `DisplaySwitch.exe` 可以切换 Windows 的显示模式；Mac 上的 `m1ddc display 1 set input 7` 可以将雷鸟 U8 切换到 Windows DP。U8 输入源读回以及 Windows ControlMyMonitor/VCP 输入控制不可靠。
- 决策：MVP 只实现两个固定场景。“切到 Mac”执行 `DisplaySwitch.exe /external`；“切到 Windows”执行 `DisplaySwitch.exe /extend` 后执行 Mac 上的 `m1ddc display 1 set input 7`。控制入口先采用 PowerShell 脚本和桌面快捷方式，不引入常驻服务、Agent、mDNS 或 WebSocket。
- 影响：MVP 假设两台主机已登录且可执行命令，依赖当前固定硬件连接和 U8 的输入值 `7`。远程唤醒、Flow 自动化、状态同步和硬件更换适配留到后续阶段。

## 待定决策

- 控制中枢运行在 Mac mini M4 还是 Windows 台式机。
- 快捷入口采用菜单栏、系统托盘、桌面悬浮窗口还是其他形式。
- 显示器输入源控制协议，以及哪些显示器纳入管理。
- Logitech Flow 自动化的实现方式和可行边界。
- Windows 远程唤醒是否纳入 MVP。
- 控制端与被控端的通信与鉴权方案。
- 配置存储方式。
- 开源协议。
- Mac 命令是否通过 SSH 在 Windows 端一键触发，还是先保留为 Mac 本地快捷入口。

## 新增决策模板

```markdown
### ADR-XXX: 决策标题

- 日期：YYYY-MM-DD
- 状态：已接受 / 暂定 / 已弃用
- 背景：触发该决策的问题。
- 决策：最终选择及理由。
- 影响：对架构、开发或用户的已知影响。
```
